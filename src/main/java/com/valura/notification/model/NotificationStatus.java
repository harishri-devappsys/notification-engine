package com.valura.notification.model;

public enum NotificationStatus {
    PENDING,
    PROCESSING,
    DELIVERED,
    FAILED,
    DUPLICATE,
    BLOCKED,
    BLOCKED_FREQUENCY,
    BLOCKED_DAILY_LIMIT

}
